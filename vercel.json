{
  "buildCommand": "cd aria && npm install && npm run build",
  "outputDirectory": "aria/dist",
  "installCommand": "echo 'skip'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
